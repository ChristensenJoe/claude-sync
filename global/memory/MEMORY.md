# Global Memory

How you work — preferences and collaboration style that apply across all projects.

Add memories here as you use Claude. The `/save-notes` command will populate this
automatically over time. Each entry should be a one-line link to its own file.

## Preferences
